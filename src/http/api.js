const HOST = "http://0.0.0.0:8091/";
const PREFIX_PATH = "api/v1000"

const REQUEST_STATE = {
    SUCCESS: "_SUCCESS",
    FAILURE: "_FAILURE",
    REQUESTING: "_REQUESTING"
}

export {
    HOST, 
    PREFIX_PATH, 
    REQUEST_STATE
};